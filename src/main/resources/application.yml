spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/devdb
    username: root
    password: root00
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
server:
  port: 5050
